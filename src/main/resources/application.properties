# Development defaults (override via env vars)
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/techstore_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=techstore_user
spring.datasource.password=Techstore123!

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# JWT (dev). In production use application-prod.properties + APP_JWT_SECRET env var.
app.jwt.secret=dGVjaHN0b3JlLWRldl9zZWNyZXRfYmFzZTY0
app.jwt.expiration-seconds=7200
